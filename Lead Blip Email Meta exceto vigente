SELECT DISTINCT
  Email;
  HasOptedOutOfEmail;
  Id;
  Status;
  Phone;
  Estado_IAL__c;
  Qual_equipamento_deseja fechar;
  Data_ultimo_Contato_;
  Interesse_de_negociação;
  CreatedDate;
  Desqualificado__c;
  LeadSource;
  LastName;
  FirstName
FROM Lead_Salesforce AS S
WHERE S.CreatedDate > '2023-01-01'
AND
S.CreatedDate < DATEADD(MONTH, -1, GETDATE())
AND
  S.LeadSource IN ('Chat - Take Blip')
  OR
  S.LeadSource IN ('Email')
  OR
  S.LeadSource IN ('Meta')
AND
S.HasOptedOutOfEmail = 'FALSE'
